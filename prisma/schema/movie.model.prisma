model Movie {
    id        Int      @id @default(autoincrement())
    title     String

    director  Director @relation(fields: [directorId], references: [id])
    directorId Int

    actors    MovieActor[]
    genres    MovieGenre[]
}
